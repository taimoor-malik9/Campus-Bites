plugins {
    alias(libs.plugins.android.application)
    alias(libs.plugins.kotlin.android)
    alias(libs.plugins.kotlin.compose)
    id 'com.google.gms.google-services'  // Add this
}
android {
    namespace 'com.example.campusbites'
    compileSdk = 36

    defaultConfig {
        applicationId "com.example.campusbites"
        minSdk 29
        targetSdk 36
        versionCode 1
        versionName "1.0"

        testInstrumentationRunner "androidx.test.runner.AndroidJUnitRunner"

        // Optional runtime-configurable AI key support (safe default is empty)
        buildConfigField "String", "AI_API_KEY", "\"\""
    }

    buildTypes {
        release {
            minifyEnabled false
            proguardFiles getDefaultProguardFile('proguard-android-optimize.txt'), 'proguard-rules.pro'
        }
    }
    compileOptions {
        sourceCompatibility JavaVersion.VERSION_11
        targetCompatibility JavaVersion.VERSION_11
    }
    kotlinOptions {
        jvmTarget = '11'
    }
    buildFeatures {
        compose true
        buildConfig true
    }
}

dependencies {
    implementation libs.androidx.core.ktx
    implementation libs.androidx.lifecycle.runtime.ktx
    implementation libs.androidx.activity.compose
    implementation platform(libs.androidx.compose.bom)
    implementation libs.androidx.compose.ui
    implementation libs.androidx.compose.ui.graphics
    implementation libs.androidx.compose.ui.tooling.preview
    implementation libs.androidx.compose.material3
    implementation libs.androidx.appcompat
    implementation libs.material
    implementation libs.androidx.activity
    implementation libs.androidx.constraintlayout

    // WorkManager (smart notifications / background work)
    implementation "androidx.work:work-runtime:2.9.0"

    // Geofencing / location for smart notifications
    implementation "com.google.android.gms:play-services-location:21.3.0"

    // Networking (AI agent) - lightweight
    implementation "com.squareup.okhttp3:okhttp:4.12.0"

    testImplementation libs.junit
    androidTestImplementation libs.androidx.junit
    androidTestImplementation libs.androidx.espresso.core
    androidTestImplementation platform(libs.androidx.compose.bom)
    androidTestImplementation libs.androidx.compose.ui.test.junit4
    debugImplementation libs.androidx.compose.ui.tooling
    debugImplementation libs.androidx.compose.ui.test.manifest
    // Firebase BOM (Bill of Materials)
    implementation platform(libs.firebase.bom)

    // Firebase products
    implementation libs.firebase.firestore
    implementation libs.firebase.auth
    implementation libs.firebase.storage

    // Optional: Firebase UI for easier authentication
    implementation libs.firebase.ui.auth
    implementation libs.cloudinary.android
    implementation libs.cloudinary.core
    implementation libs.glide
    annotationProcessor libs.compiler
    // Add Gson for JSON serialization/deserialization
    implementation 'com.google.code.gson:gson:2.10.1'

}